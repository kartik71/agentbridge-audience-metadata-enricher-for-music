{
  "name": "agentbridge-audience-metadata-enricher-for-music",
  "version": "1.0.0",
  "description": "End-to-end agentic workflow to moderates edits, schedules rights DB, and drive accelerate post through Ad Server.",
  "main": "src/index.js",
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "build": "npm run compile",
    "compile": "tsc",
    "test": "jest",
    "lint": "eslint src/**/*.{js,ts}",
    "deploy": "npm run deploy:oci",
    "deploy:oci": "echo \"Deploy to OCI\" && npm run build",
    "docker:build": "docker build -t agentbridge-blueprint .",
    "docker:run": "docker run -p 3000:3000 agentbridge-blueprint"
  },
  "keywords": [
    "agentbridge",
    "ai",
    "agents",
    "reflection",
    "media-&-entertainment",
    "localization",
    "post",
    "reflection",
    "media"
  ],
  "author": "AgentBridge Team",
  "license": "MIT",
  "dependencies": {
    "@agentbridge/core": "^1.0.0",
    "@agentbridge/mcp": "^1.0.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "eslint": "^8.0.0",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}